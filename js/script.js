window.addEventListener("DOMContentLoaded", () => {
    let io = document.getElementById("finresult")
    if (.prenumber >= 70) {
        io.style.backgroundColor = 
    }

    //F to C (valNum-32)/1.8
    //C to F (valNum*1.8)+32

    /* add listener for input,
    use radio to choose which conversion,
    output box display finresult/postnumber (unsure)
    clear button to reset page