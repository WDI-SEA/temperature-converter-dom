console.log('Hello, front end');

// this is the conversion for Farenheit to Celcius
const FarToCel = () => {
    let temp = document.getElementById("temp") => {
        let tempResult = ('temp' / 5) * 9 - 32 
    }
    console.log(tempResult)
}


// Thi is the conversion for Celcius to Farenheit
const CelToFar = () => {
    let temp = document.getElementById("temp") => {
        let tempResult = ('temp' - 32) * 5 / 9 
    }
    console.log(tempResult)
}


//sorry I really tried, will need help in the morning SOS